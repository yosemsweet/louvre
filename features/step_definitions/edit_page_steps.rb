Given /^the page has (\d+) widgets$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^I click and drag a widget to a new spot on the page$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^the page should save the changes$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^the page has a widget$/ do
  pending # express the regexp above with the code you wish you had
end

When /^WYSIWYG form appears with the Widget text pre\-populated$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I fill in "([^"]*)"$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Then /^"([^"]*)" should appear on the page$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^click "([^"]*)"$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Then /^"([^"]*)" should be "([^"]*)"$/ do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

