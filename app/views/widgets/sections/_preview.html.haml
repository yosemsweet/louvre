- case widget.content_type

- when 'text_content'
  = HTML_Truncator.truncate(widget.text, 140, :length_in_chars => true).html_safe

- when 'image_content'
  %img{ :src => "#{widget.image}", :alt => "#{widget.alt_text}", :title => "#{widget.alt_text}"}
