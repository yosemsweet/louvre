require "spec_helper"

describe UserMailer do

  describe 'notifications' do
      #     before (:each) do
      # user = Factory.build(:user)
      # user.emails.address = 'lucas@email.com'
      # user.save
      # event = Factory.create(:event)
      # let(:mail) { UserMailer.notifications_email(user,event) }
      #      end
      # 
      #     
      # 
      #     #ensure that the subject is correct
      #     it 'renders the subject' do
      #       mail.subject.should == 'Loorp Updates'
      #     end
      # 
      #     #ensure that the receiver is correct
      #     it 'renders the receiver email' do
      #       mail.to.should == [user.emails.first.address]
      #     end
      # 
      #     #ensure that the sender is correct
      #     it 'renders the sender email' do
      #       mail.from.should == ['noreply@loorp.com']
      #     end
      # 
      #     #ensure that the @name variable appears in the email body
      #     it 'assigns @name' do
      #       mail.body.encoded.should match(user.name)
      #     end

    #ensure that the @confirmation_url variable appears in the email body
    # it 'assigns @confirmation_url' do
    #   mail.body.encoded.should match("http://aplication_url/#{user.id}/confirmation")
    # end
 
  end
  
end
